"use strict";
exports.__esModule = true;
function main() {
    //AsteroidCollision();
    var sampleArray = [1, 2, 3, 4, 5];
    var hap = sampleArray.map(function (unit) {
        return unit + unit;
    });
    console.log(hap);
}
main();
