import { RouteType, PageType } from "./types";

function Router(): string {
  const component: string = "<div>test</div>";
  // const { pathname } = location;

  return component;
}

export default Router;
