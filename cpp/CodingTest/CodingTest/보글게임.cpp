#include<iostream>

using namespace std;

bool hasWord(int y, int x, const string& word) {

}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int w;
	cin >> w;
	string word;
	for (int i = 0; i < w; i++) {
		cin >> word;
	}


	return 0;
}